import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    height: '100%',
  },
  content: {
    'margin-top': 20,
    'margin-bottom': 20,
  },
}));

export default useStyles;
